import { Button } from 'antd'
import React from 'react'
import { jwt } from '../../../api/api'

const Home = () => {

    return (
        <div>
           <Button >click</Button>
        </div>
    )
}

export default Home
